import { useState } from 'react';
import { Layout } from '../../Components/Layout';

function NotFound() {
  return (
    <Layout>
      NotFound Page (404)
    </Layout>
  )
}

export default NotFound;